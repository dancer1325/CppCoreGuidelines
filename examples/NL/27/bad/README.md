# if you include >1 time the WRONGLY specified `.h` -> you get a linker error
* run [main.cpp](main.cpp)
  * ways
    * -- via -- IDE
    * -- via -- CL tool
      * `clang++ -o main main.cpp`
  * check you get the error